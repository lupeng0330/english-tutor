# 三大书写/词汇题型 · 重设计方案（待确认）

> 面向低段 8 题型模板中的 **单词拼写 / 连词成句 / 完成句子**。
> 目标：先把「交互 + 数据结构 + 判分」设计定稿并让你确认，再统一改代码 + 各年级扩充题量。
> 现状版本：3 上期中已用低段 8 题型模板（50 题 / 100 分），但这 3 个题型渲染逻辑有 Bug。

---

## 一、现状问题诊断（根因）

| 题型 | 你看到的现象 | 根因（代码层） | 现有题量 |
|---|---|---|---|
| **单词拼写** `spelling` | 有中文词义，但**没有地方填英文** | 题库是「中文→拼英文」结构（`q`=中文、`answer`=英文、`hint`=首字母提示），却被当**选择题**渲染 `_renderQuestionHTML`（遍历 `options`，而它为空）→ 只显示中文；判分还按「选项索引」比对（完全错位） | **645 条（充足）** |
| **连词成句** `sentence_order` | **上方没有可点的单词** | 抽题时（`exam.js` 新题型分支）只拷贝了 `q/options/answer`，**漏拷 `words` 字段** → 渲染函数里 `q.words` 为空 | **仅 1 条** |
| **完成句子** `blank_fill` | **只有标题、没有题干、无处作答** | 题库是 `passage + blanks[]` 结构（没有顶层 `q`/`options`），却被当普通选择题渲染 → 题干 `q` 为空、无输入框；也无判分 | **仅 1 条** |

结论：`spelling` 是**纯逻辑问题**（题库够用）；`sentence_order` / `blank_fill` 是**逻辑 + 题量双缺**。

---

## 二、重新设计（数据结构 + 交互 + 判分）

### 题型 1 · 单词拼写 `spelling`（中译英拼写）

**数据结构（沿用现有，无需重造）**
```json
{
  "grade": 3, "term": "上", "code": "3A_U1",
  "q": "苹果",              // 中文词义（题面提示）
  "answer": "apple",         // 正确英文单词
  "hint": "a____",          // 首字母 + 字母数提示
  "difficulty": 1, "explain": "名词：苹果"
}
```

**交互**
- 题号 + 中文词义（大字）+ 首字母提示（灰色小字，如 `a ＿＿＿＿`）
- 一个**单行文本输入框**，输入英文单词
- 答案存 `answers[index] = "用户输入的字符串"`

**判分**
- `normalize(输入) === normalize(answer)`；`normalize` = 去首尾空格 + 转小写
- 建议：**忽略大小写**、忽略首尾空格；单词题不涉及标点

---

### 题型 2 · 连词成句 `sentence_order`

**数据结构（建议标准化——比现有更直观、更好写题、判分更稳）**
```json
{
  "grade": 3, "term": "上", "code": "3A_U1", "type": "sentence_order",
  "words": ["apple", "an", "is", "This", "."],   // 打乱后的词块（用于候选池展示）
  "answer": "This is an apple .",                  // 正确句子（完整字符串）
  "difficulty": 1, "explain": "陈述句语序：主+谓+宾"
}
```
> 变化点：现有样例是「`words` 正序 + `answer` 索引数组」，改为「`words` 乱序 + `answer` 正确句子」。
> 好处：出题人只写打乱词 + 正确句即可，无需手算索引；判分直接比句子文本，不受重复词干扰。

**交互**
- 题号 + 提示语
- **候选词池**：把 `words`（乱序）渲染成一排可点词块
- 点词 → 进入下方「答案区」拼句；再点答案区词可移回；「清空重排」按钮
- 答案存 `answers[index] = [已点选的词索引序列]`

**判分**
- 把点选序列拼成句子 → `normalize` 后 === `normalize(answer)`
- `normalize` = 合并多余空格 + 转小写（句首大小写不计较，标点保留）

---

### 题型 3 · 完成句子 `blank_fill`

**数据结构**
```json
{
  "grade": 3, "term": "上", "code": "3A_U1", "type": "blank_fill",
  "passage": "I have a ____. It is ____.",   // 带 ____ 的句子/短句
  "blanks": [
    {"answer": "dog",  "options": ["dog", "cat", "bird"]},   // options 可选
    {"answer": "cute", "options": ["cute", "big", "small"]}
  ],
  "difficulty": 1, "explain": "描述宠物"
}
```

**交互（3 种呈现，见待确认项）**
- 把 `passage` 中每个 `____` 就地替换为一个作答控件：
  - **A 选词填空**：每个空是候选词按钮/下拉（低段友好，需 `options`）
  - **B 直接填空**：每个空是行内输入框（更难，可给首字母提示）
  - **C 混合（推荐）**：该空**有 `options` → 选词**，**无 `options` → 输入框**
- 答案存 `answers[index] = ["空1作答", "空2作答", ...]`

**判分（按空给分，推荐）**
- 逐空比对，本题得分 = 本题满分 × (答对空数 / 总空数)
- 或「全对才给分」（待确认）

---

## 三、判分 / 计分对齐

- 三题型全部接入现有 `_gradeExam`：`spelling`/`blank_fill` 走**文本比对**，`sentence_order` 走**句子比对**。
- 仍受 **P1 总分锁定**约束：每个 section 满分 = 配置 `count × points`，整卷严格 100，不溢出。
- 结果页 / 错题本：三题型正确答案以「文本」形式回显（而非 A/B/C 选项）。

---

## 四、各年级扩充题量计划（确认设计后执行）

储备标准：**每题型 · 每学期 ≥ 单卷抽取量 × 5**（保证多次刷卷不重复）。

| 题型 | 单卷抽取 | 每学期储备目标 | 现状 | 计划 |
|---|---|---|---|---|
| 单词拼写 `spelling` | 低段 10 | ≥ 50 | 645 条（低段每学期 70）✅ | **无需新增**，仅校验中文词义/hint 规范 |
| 连词成句 `sentence_order` | 低段 5 | ≥ 25 | 1 条 🔴 | 低段 3上/下、4上/下 4 学期 → **≥ 100 条** |
| 完成句子 `blank_fill` | 低/高段 5 | ≥ 25 | 1 条 🔴 | 低段 4 学期 →≥100；如高段也用，再 +100 |

**扩充范围待确认**：先把低段（3–4 年级）跑通，还是低/高段一起铺？

---

## 五、请你确认的决策点

1. **单词拼写**：显示 `hint` 首字母提示？答案忽略大小写？（建议：都✅）
2. **连词成句**：采用新规范「`words` 乱序 + `answer` 正确句子」？（建议✅，我会重写现有 1 条样例并据此扩题）
3. **完成句子**：呈现选 **A 选词 / B 填空 / C 混合**？（建议 **C**）；判分「按空给分」还是「全对才给分」？（建议 **按空给分**）
4. **扩充范围**：先低段（3–4）跑通，还是低/高段一起？每学期储备目标是否用「25 条/题型/学期」？

> 你逐条回复上面 4 点即可（可只说与建议不同的项，其余默认采纳建议）。确认后我：
> ① 改 `exam.js`（3 题型渲染 + 抽题带字段 + 判分）→ ② 本地双端验证 → ③ 你验收 → ④ 自动部署 + 更新 `PROJECT_STATUS.md`，随后按确认范围扩充题量。
