# jk 教科版 剩余 7 册补齐 · 详细开发任务单

> 目标：把教科版 jk **3 下 / 4 上下 / 5 上下 / 6 上**（共 7 册）补齐**课文 + 单词卡 + 例句 + 例句 mp3**，达到与 3 上同等完成度。  
> 起点：2026-06-26（紧接 §22 上线后）  
> 路径：**全量复用 3 上已验证的"颗粒极小 + 可断点续跑"流水线**（详见 `PROJECT_STATUS.md §22`）。  
> 状态：✅ **决策项已确认（2026-06-26 21:35 用户勾选）**，开发宪法生效；批次 1 待开工。

---

## 0. 现状盘点（已查实，盘点时间 2026-06-26 21:25）

| 年级·学期 | jk.json 单元数 | 词总数 | 课文键 | 例句 | 判定 | 本计划处理 |
|---|---|---|---|---|---|---|
| grade1 上下 | 2 / 2 | 10 / 10 | `lesson`(旧) | ❌ | 占位 | **跳过**（教科版三年级起点，1-2 年级走 `gzk`） |
| grade2 上下 | 2 / 2 | 10 / 10 | `lesson`(旧) | ❌ | 占位 | **跳过**（同上） |
| **grade3 上** | **9** | **70** | `lessons[]` | ✅ 210/210 | **真实 ✅** | 已完成 §22 |
| grade3 下 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 1** |
| grade4 上 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 2** |
| grade4 下 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 3** |
| grade5 上 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 4** |
| grade5 下 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 5** |
| grade6 上 | 2 | 10 | `lesson`(旧) | ❌ | 占位 | **批次 6** |
| grade6 下 | 11 | 106 | `lessons[]` | ✅ 493/164 | **真实 ✅** | 不动（旧 2014 版） |

例句文件现有 8 个（hj 6 册 + jk 6下 + jk 3上）。

---

## 1. 已确认决策（2026-06-26 21:35）

> 通过铁律 5 的"选项清单"一次性收集 4 项决策，全部按推荐项选定。

| # | 决策项 | 已选 | 影响 |
|---|---|---|---|
| Q1 | 教材版本 | **A · 全部用 2024 秋新版** | 7 册目录/词表按 2024 新版核对；6 下后续单独排期重做 |
| Q2 | 单册容量 | **接受默认量级**（8+1 复习 / 70-96 词 / 18-20 课文 / 210-288 例句 / 册） | 7 册总量约 63 单元 / 544 词 / 1632 例句 / 1655 mp3 |
| Q3 | 推进节奏 | **逐册推进**（每批 1 册，单册 ~2 小时净时间） | 每批失败可精准排障；总周期约 2 周（日 1 册）或 2-3 个周末 |
| Q4 | 提效项（多选） | **§3.1 脚本模板化 + §3.3 通用 verify + 任务单 push 上线 + 铁律 5 入文档** | 后续每册省 5 分钟/册 + 减少改错风险；开发宪法生效 |

### 各册规模估算

| 年级 | 预估单元数 | 预估词数 | 预估课文数 | 预估例句数 | 预估新增 mp3 |
|---|---|---|---|---|---|
| grade3 下 | 8+1 复习 | ~72 | ~18 | ~216 | ~210 |
| grade4 上下 | 8+1 复习 / 册 | ~80 / 册 | ~18 / 册 | ~240 / 册 | ~230 / 册 |
| grade5 上下 | 8+1 复习 / 册 | ~88 / 册 | ~20 / 册 | ~264 / 册 | ~250 / 册 |
| grade6 上 | 8+1 复习 | ~96 | ~20 | ~288 | ~275 |
| **7 册合计** | **~63 单元** | **~544 词** | **~132 课文** | **~1632 例句** | **~1655 mp3** |

---

## 2. 单册标准流水线（按 3 上验证版本复用，每册 19 步）

每册建立独立目录 `scripts/<gradeN>_{shang|xia}/`（命名约定：`g3x` / `g4s` / `g4x` / `g5s` / `g5x` / `g6s`），目录下沿用：

```
scripts/g4s/                            ← 示例
├── u1.json ~ u9.json                   ← 单元草稿（每单元独立文件）
├── e1.json ~ e9.json                   ← 例句草稿（每单元独立文件）
├── import_textbook.py                  ← 从 3 上复制，改路径常量与目标 grade/term
└── merge_examples.py                   ← 从 3 上复制，改路径常量
```

### 单册 19 步（与 §22 一致）

| # | 阶段 | 步骤 | 工具 | 单步上限 |
|---|---|---|---|---|
| 1 | 准备 | **写 u1-u9.json** 单元草稿（话题、词表、2 篇课文）| 人工编纂（参照 2024 新版电子课本目录核对单元名/词表） | 1 单元 / 步 |
| 2 | 准备 | 校验 9 个 u*.json 字段完整性 | `_verify.py`（一次性临时脚本） | 1 步 |
| 3 | 准备 | 写 `import_textbook.py`（复用 3 上版本）+ dry-run 打印 diff | python | 1 步 |
| 4 | 准备 | --write 实际写入 + `_verify_jk.py` 校验 | python | 1 步 |
| 5-13 | 例句 | **ex-u1 ~ ex-u9**：每单元 8 词 × 3 句易/中/难 | 人工编纂 | 1 单元 / 步（≤27 句） |
| 14 | 合并 | 跑 `merge_examples.py` → `jk_grade{N}_{term}.json` | python | 1 步 |
| 15 | 音频 | 把 `jk_grade{N}_{term}.json` 加进 `gen_example_audio.py` 的 `EX_FILES` | 1 行改动 | 1 步 |
| 16-18 | 音频 | `gen_example_audio.py --only ... --limit 80` 跑 **3 批**（80/80/剩余） | python（edge-tts） | 5-7 分钟 / 批 |
| 19 | 收尾 | 双端验证 → 更新 `PROJECT_STATUS.md` 同步章节 → `dev-push.ps1` 上线 | preview_url + 文档 + push | 1 步 |

**关键防卡死参数**：
- `import_textbook.py`：先 dry-run 后 --write，自动 .bak 备份
- 例句：**单步只动 1 单元**，最多 27 句
- 音频：`--limit 80`，单批 ≤7 分钟，中断后下次自动续跑（hash 去重）
- `git pull --rebase` 推送前必做

---

## 3. 提效项 / 可选优化（不影响正确性）

### 3.1 一次性把脚本模板化 ✅ 已采纳
3 上目录 `scripts/g3s/` 下 `import_textbook.py` / `merge_examples.py` 当前路径常量硬编码。重构为：

```python
# scripts/_jk_volume_lib.py  （新增·共享库）
def import_units(here_dir, grade, term, jk_path):  ...
def merge_examples(here_dir, out_path, jk_path, grade, term):  ...
```

后续每册只需写**两个薄入口文件**（6 行左右指定路径/年级/学期），不再复制粘贴主逻辑。

### 3.2 音频默认 `--limit 80`
当前 `gen_example_audio.py` 已支持 `--only` / `--limit`，**不需要改**。后续每册沿用三批节奏：

```bash
python gen_example_audio.py --only jk_grade4_shang.json --limit 80   # 批1
python gen_example_audio.py --only jk_grade4_shang.json --limit 80   # 批2
python gen_example_audio.py --only jk_grade4_shang.json              # 收尾
```

### 3.3 单批校验（写入完成后立即跑）
每批完成后用同款一行 python 检查：
```python
词数 与 jk.json[grade][term] 总词数一致；
句数 = 词数 × 3；
audioFile 字段全有；
audio/ex_*.mp3 全部落盘。
```

已固化为 `scripts/_verify_volume.py grade term` ✅ 已采纳，**只写 1 次**，后续 6 册免重复。

---

## 4. 时间预估（不含等待你确认的时间）

| 项 | 单批耗时 | 7 册合计 |
|---|---|---|
| 单元草稿（u*.json） | 60-80 分钟 | **7-9 小时** ← 主要瓶颈（核对官方目录 + 编纂改编课文） |
| 例句草稿（e*.json） | 30-40 分钟 | **3.5-5 小时** |
| 脚本复用 + 导入 + 合并 | 5 分钟 | 35 分钟 |
| 音频生成（edge-tts） | 15-20 分钟 / 批 | **2-2.5 小时** |
| 双端验证 + 文档 + push | 5 分钟 | 35 分钟 |
| **单册净耗时** | **~2 小时** | **14-17 小时**（纯净流水线） |

> 这是"流水线净时间"。若你节奏是**每天 1 册**，约 **2 周完成 7 册**；若**周末集中**，2-3 个周末跑完。

---

## 5. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| ⚠️ 2024 新版各册官方目录公开度差异 | 个别册词表/单元题目核对慢 | 用 `dzkbw.com` / `dzskb.com` 等公开电子课本核对；查不到的临时按"上一册延续 + 同年级人教/外研类似话题"启发式列草案给你确认 |
| ⚠️ 编纂 200+ 句例句反复修改 | 拉长单册时间 | 仍按 3 上**单元一文件**原则；改一个单元只重生该单元的 e*.json，整体不影响 |
| ⚠️ edge-tts 偶发网络失败 | 个别 mp3 缺失 | 脚本已含 hash 去重 + 增量跳过，重跑零浪费 |
| ⚠️ jk.json 体积膨胀（目前 121KB，加 7 册后预计 ~600KB） | 首屏加载略慢 | 复用既有 `scripts/split_textbook_by_grade.py` 把 jk.json 按年级分片（同 hj_grade{7,8,9}.json 方案） |
| ⚠️ 6 下仍是 2014 旧版，与新 6 上风格冲突 | 学生切册视觉不一致 | 列入"P2 后续技术债"，本 7 册任务暂不动 6 下；待 7 册完成后单独排期重做 6 下 |

---

## 6. 推荐的最小化首动作（**确认后立刻能开工**）

只需你回答 §1 的 3 个问题（或直接说"按推荐项")，我立刻：

1. **批次 1（3 下）开工**：
   - 写 `scripts/g3x/u1.json ~ u9.json` 草稿（基于 2024 新版 3 下电子课本目录）
   - 给你一份 `JK_G3_XIA_DATASET.md`，列出单元结构 + 目标词表，**给你勾选确认**（同 3 上模式）
   - 你确认词表 → 我补音标/释义/例句/课文 → 写入 → 生成 mp3 → 双端验证 → push

2. （可选）**同步做 §3.1 脚本模板化**（30 分钟），让后续 6 册每册省 5 分钟 + 减少改错风险

---

## 7. 进度追踪表（每完成一册划掉一项）

| 批次 | 册 | 单元草稿 | 数据集确认 | 词卡/课文完成 | 例句完成 | mp3 完成 | 上线版本 |
|---|---|---|---|---|---|---|---|
| 1 | 3 下 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| 2 | 4 上 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| 3 | 4 下 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| 4 | 5 上 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| 5 | 5 下 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| 6 | 6 上 | ☐ | ☐ | ☐ | ☐ | ☐ | — |
| —（已完成参考） | **3 上** | ✅ | ✅ | ✅ | ✅ | ✅ | `20260626V02.22` |

---

## 8. 给"另一端的 AI 助手"的执行约定

1. 严格按 §2 的 19 步顺序，**每一步独立 commit 不合并**（出问题可精准 revert）。
2. 单元草稿 `u*.json` 与 dataset `.md` **分离**：dataset 给用户确认词表，u*.json 是最终入库源数据，避免边修边改。
3. 推送前必跑 `git pull --rebase origin main`，推送后必更新 `PROJECT_STATUS.md` 对应章节（铁律 2 + 4）。
4. 音频生成默认 `--limit 80`，**绝不一次跑完整册**（防卡死 + 防中断丢进度）。
5. 6 下不动；1-2 年级不动；只动表中列出的 7 册。

---

_此任务单可直接 push 到仓库，作为 7 册分批交付的"开发宪法"。每完成一册，更新 §7 进度表并补 `PROJECT_STATUS.md` 完成记录。_
